<?xml version="1.0"?>
<ruleset name="Custom Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        Generated by Korekto
    </description>

    <rule th:each="rule : ${rules}"
          th:attr="ref=${rule.ref != null}?${rule.ref}, class=${rule.clazz != null}?${rule.clazz}, name=${rule.name != null}?${rule.name}, message=${rule.message != null}?${rule.message}, language=${rule.clazz != null}?@{java}">
        <properties th:unless="${#maps.isEmpty(rule.configuration)}">
            <property th:each="prop : ${rule.configuration}" th:name="${prop.key}" th:value="${prop.value}"/>
        </properties>
    </rule>
</ruleset>
